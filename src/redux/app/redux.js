const initialState ={

}

export const Action = {
    Types:{

    },
    Creators:{

    }
}

export const reducer = (state = initialState, action) => {
    switch(action.type){
        default: return state;
    }
}